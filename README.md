# 🚀 Mac Setup Script

Este repositorio contiene un **script modular de setup para macOS**.  
Permite instalar apps, herramientas de desarrollo, configurar terminal y ajustar el sistema.

## Requisitos
- macOS (Intel o Apple Silicon)
- Conexión a internet
- bash o zsh (viene por defecto)

## Uso
```bash
chmod +x setup.sh
./setup.sh --apps
./setup.sh --dev
./setup.sh --terminal
./setup.sh --productivity
./setup.sh --system
./setup.sh --all
